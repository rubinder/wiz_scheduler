# Test-only dependencies. Kept separate from requirements.txt so they
# don't bloat the production Docker image. Install both files for local
# dev:  `uv pip install -r backend/requirements.txt -r backend/requirements-dev.txt`

pytest>=8.0.0
pytest-asyncio>=0.23.0

# Test database driver — conftest.py uses sqlite+aiosqlite:/// so the
# suite runs without a Postgres instance.
aiosqlite>=0.19.0
